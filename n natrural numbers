Input = 5

Output = 1,2,3,4,5

program 

#include <stdio.h>

int main() {
    int n,i;
    scanf("%d",&n);
    i=1;
    while(i<=n)
    {
        printf("%d ",i);
         i++;
    }

    return 0;
}
